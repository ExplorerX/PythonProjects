函数：可读性强，复用性强
定义：
def 函数名()
    函数体
    return 返回值
调用
所有函数，只有定义不调用就一定不执行
所有函数，先定义后调用

函数名() # 不接收返回值
返回值 = 函数名()  # 接收返回值

返回值有三种情况：
    1、没有返回值：默认返回None
        不写return，函数内部的代码执行完毕函数自动结束
        只写return，结束一个函数或者返回值
        return None
    2、返回一个值：结束了函数，并且返回一个值，可以是任意值
    3、返回多个值：多个值之间用逗号隔开，接收的时候可以用一个变量接收，也可以用等量的多个变量接收
       用一个变量接收，返回的是一个元组

参数：
    形参：定义函数的时候
        位置参数：必须传
        *args：动态参数，可以接收任意多个按位置传入的参数
        默认参数：可以不传
        **kwargs：动态参数，可以接收任意多个按关键字传入的参数
    实参：调用函数的时候
        1、按照位置传参数
        2、按照关键字传参数
        3、可以混用，位置参数必须在关键字参数之前
            不能对一个参数重复赋值
    动态参数的另一种传入方式：
        传入列表或者字典