1.昨日内容及作业讲解
    ascii：字母，数字，特殊字符：1个字节8位表示 最多表示256个
    Unicode：16位，两个字节表示  升级之后使用32位  4个字节表示 因为汉字有9W多个，16位表示不完
    utf-8：最少一个字节8位表示。英文字母：8位，一个字节
                              欧洲：16位，两个字节
                              中文：24位，三个字节
    还有utf-16和utf-32
    gbk：表示英文用一个字节八位表示，中文用两个字节十六位表示，中国人基于ascii码制作

    数据类型：
    int：bit_length():转换成二进制的最少位数
    bool：True和False
    str：str ->bool：str(bool):'' -> False
    str:索引和切片：
    取单个字符s[1];
    切片s[1:3]顾头不顾尾
    全取：s[:]或者s[0:]
    步长：s[0:3:2]第三位是步长
    倒过来取步长是2：s[2::-2]

    字符长常用操作：
    首字母大写：capitalize()
    全大写：upper()
    全小写：lower()
    查找：find()通过元素找索引，找不到返回-1
    查找：index()通过元素找索引，找不到报错
    大小写反转：swapcase()
    求长度：len()
    替换：replace()old，new，count
    判断以什么开头：startwith()，有切片功能
    计数：count()有切片，计算出现的次数
    使用特殊字符隔开的首字母大写：title()
    居中：center()
    删除特殊字符：strip(),也可以从左从有删除lstrip()和rstrop()
    字符串分割：spilt()
    格式化输出：foamat()三种，占位符是{}

    for i in 可迭代对象
     pass
   

